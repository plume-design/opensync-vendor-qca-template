#include <stdio.h>
#include <stdbool.h>

bool target_dummy_example()
{
    /*
     * An example showing how to use per-TARGET overrides for
     * target functions implemented in the OpenSync core layer.
     */
    return true;
}
